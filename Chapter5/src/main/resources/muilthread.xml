<?xml version="1.0" encoding="UTF-8" ?>
<suite name="thread" parallel="classes" thread-count="2" >
    <!--
    tests级别：不同test tag下的用例可以在不同线程下执行，相同test tag下的用例只能在同一个线程中执行

    classes级别：不同classes tag下的用例可以在不同线程下执行，相同classes tag下的用例只能在同一个线程中执行

    methods级别：所有用例都可以在不同线程下执行

    thread-count代表了最大并发线程数

    xml文件配置不能指定线程池
    -->
    <test name="demo1">
        <classes name="q1">
            <class name="com.course.testng.MuitThread.muilthreadomxml"/>
        </classes>
    </test>

    <test name="demo2">
        <classes name="q2">
            <class name="com.course.testng.MuitThread.muilthreadomxml"/>
        </classes>
    </test>

</suite>